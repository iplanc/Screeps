var stuffTools      = require('stuff.tools');

var expandShard = '';
var targetShard = '';

var autoExpandExt = {
    run: function() {
        if (expandShard != '' && targetShard != '') {
            
        }
    }
}

module.exports = autoExpandExt;